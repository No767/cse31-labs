TPS 1:

1. This is loaded and present

2. Also loaded and present 

3. What we need to do is to declare our string, and load that into $a0, and print via the command code $v0, and do the same thing but with the command code of 4 to take in an integer

4. As seen from here: https://student.cs.uwaterloo.ca/~isg/res/mips/traps
    Effectively we use the trap code 4 to print our string, 5 to read an int, and then move it from $v0 to $t3

5. No. The "n" variable is declared to loop through "n" amount of times. in this case, it's always 13 (unless modified, which it isn't here). We also have to remove the associated "li" and "la" instructions, because it will only loop 13 times

6. All we need to do is to comment out line 2 (where we declare our "n" variable) and lines 19-20, where we load the data in
   Now if an user inputs a number such as "5", they will get the result of "5". The number "15" results in the number "620"

TPS 2:

1. See below:

    Call recursion(5): Needs recursion(3) + recursion(4)
        Call recursion(3): Needs recursion(1) + recursion(2)
            Call recursion(1): Needs recursion(-1) + recursion(0)
                Call recursion(-1): BASE CASE -> Returns 1 (Prints "Returning 1")
                Call recursion(0): BASE CASE -> Returns 3 (Prints "Returning 3")
            recursion(1) resolves to 1 + 3 = 4
            Call recursion(2): Needs recursion(0) + recursion(1)
                Call recursion(0): BASE CASE -> Returns 3 (Prints "Returning 3")
                Call recursion(1): Needs recursion(-1) + recursion(0)
                    Call recursion(-1): BASE CASE -> Returns 1 (Prints "Returning 1")
                    Call recursion(0): BASE CASE -> Returns 3 (Prints "Returning 3")
                recursion(1) resolves to 1 + 3 = 4
            recursion(2) resolves to 3 + 4 = 7
        recursion(3) resolves to 4 + 7 = 11

        Call recursion(4): Needs recursion(2) + recursion(3)
            Call recursion(2): Needs recursion(0) + recursion(1)
                Call recursion(0): BASE CASE -> Returns 3 (Prints "Returning 3")
                Call recursion(1): Needs recursion(-1) + recursion(0)
                    Call recursion(-1): BASE CASE -> Returns 1 (Prints "Returning 1")
                    Call recursion(0): BASE CASE -> Returns 3 (Prints "Returning 3")
                recursion(1) resolves to 1 + 3 = 4
            recursion(2) resolves to 3 + 4 = 7
            Call recursion(3): Needs recursion(1) + recursion(2)
                Call recursion(1): Needs recursion(-1) + recursion(0)
                    Call recursion(-1): BASE CASE -> Returns 1 (Prints "Returning 1")
                    Call recursion(0): BASE CASE -> Returns 3 (Prints "Returning 3")
                recursion(1) resolves to 1 + 3 = 4
                Call recursion(2): Needs recursion(0) + recursion(1)
                    Call recursion(0): BASE CASE -> Returns 3 (Prints "Returning 3")
                    Call recursion(1): Needs recursion(-1) + recursion(0)
                        Call recursion(-1): BASE CASE -> Returns 1 (Prints "Returning 1")
                        Call recursion(0): BASE CASE -> Returns 3 (Prints "Returning 3")
                    recursion(1) resolves to 1 + 3 = 4
                recursion(2) resolves to 3 + 4 = 7
            recursion(3) resolves to 4 + 7 = 11
        recursion(4) resolves to 7 + 11 = 18

    recursion(5) resolves to 11 + 18 = 29

2. The "main" function in the recursion.s file is incomplete, but based off it's counterpart, the MIPS version only sets up the stack, saves the stack pointer into $s1, and calls the recursion label, and ends once it reaches the end of the program
    Compared to it's C counterpart, the C version prints out a prompt, reads an integer, and calls the recursion function, and ends. All of the setup is done internally under the hood

3. Our prompt is declared within the variable "str1", and is printed out in a standard fashion

4. It should go into the $a0 register, because it will be used for a function call instead

5. All return values should be stored in the $v0 register. Naturally, $v0 is also used for syscall codes, we need to pass our final answer back into $a0, as this will be the new input for the recursive loop

6. The final value (our "29") is sitting in $v0, so we need to move it to $a0 first to be sent for printing

7. We need to save the following three registers:

- $ra (So we can actually go back to the correct return address)
- $a0 (Our original argument. In the C code, this is our argument "m", as "m - 2" would be the new argument instead)
- $s0 (This will be our saved value, pulled from $v0)

Thus, we need to put 12 bytes into the stack, with $ra being the first thing, with $a0 and $s0 following

8. All we need to do is set the return value ($v0) to 1 (via the "li" instruction) and jump to end_recur

9. Likewise with Question 8, as this is the base case for when m == 0, we do the same thing, by setting $v0 to 3, and jump to end_recur

10. $a0 is going to get overridden. As we need to prepare for the second call, it would be $a0 += -2. I.e., this means that $a0 would be overridden with the new result.

12. If we call the second recursion, $v0 would be overwritten and lost. $v0 gets used by all of the recursive calls and $v0 gets used, so we wouldn't be able to add the two results back together.
    We can solve this by shoving it in a safe register. We already saved our original value of $s0 into the stack, so we can put it in $s0 as the value for it is now unused and could be written to

13. We need to get the original "m" value (in $a0) because it is tainted throughout the other calls. And then, do m - 1, and send it back for another recursion loop

14. Our original result is located in $s0 (in Question 12, we set that up). So we will just update our return value (sitting in $v0) by appending $s0 to it. I.e., $v0 += $s0

15. We need to get our original $ra address, as we tainted it throughout our recursive calls. Also grab our original $s0, as that is now tainted by our recursive calls (See TPS 2, Question 12) 